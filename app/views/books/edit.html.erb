<div>
  <%= flash[:notice] %>
</div>

<h2>Editing Book</h2>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj form being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>

<%= form_with model: @book do |f| %>
  <label for="inputName">Title</label><br>
  <%= f.text_field :title, autofocus: true, id:"inputName", placeholder:"title"%><br>
  <label for="inputName">Opinion</label><br>
  <%= f.text_area :body, autofocus: true, id:"inputName", placeholder:"body"%><br>
  <%= f.submit "Update Book" %><br>
  <%= link_to "Show", book_path(@book.id) %>  
  <a class="back" href="/books">Back</a>
<% end %>